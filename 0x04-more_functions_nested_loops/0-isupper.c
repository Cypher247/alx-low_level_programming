#include "main.h"
/**
 * _isupper -prog checks for capital characters
 * @c: Variable text
 * Return: Always 0.
 */
int _isupper(int c)
{
if (c >= 'A' && C <= '2')
}
return (1);
}
else
{
return (0);
}
}
